# 모든 작업에 적용하는 영구 원칙

- 사용자 입력에 오타, 축약어 또는 여러 의미로 해석될 수 있는 표현이 있으면 임의로 뜻을 확정하지 않는다.
- 답변이나 작업 전에 어떤 표현을 무엇으로 이해했는지 정확히 밝히고 사용자에게 확인한다.
- 해석에 따라 답이나 실행 결과가 달라질 수 있으면 확인받기 전에는 진단, 코드 수정, 설정 변경을 진행하지 않는다.
- 사용자의 최신 질문에 먼저 직접 답하고, 관련 설명은 그 다음에 덧붙인다.
- 애매하거나 책임 범위를 흐리는 표현을 사용하지 않는다. `대체로`, `사실상`, `의도는 아니었다`, `문제없어 보인다`, `정상인 것 같다`처럼 검증 범위를 불명확하게 만드는 표현은 금지한다.
- 사용자와 작업자의 의도를 임의로 만들거나 추정하지 않는다. 의도는 당사자가 명시한 경우에만 인용하고, 그 외에는 실행된 명령·코드·응답·결과처럼 확인 가능한 사실만 말한다.
- `확인됨`, `미확인`, `부분 완료`, `실패`, `검증 불가`를 명확히 구분한다. 미확인 사항을 완료나 정상으로 확대해서 표현하지 않는다.
- 완료라고 답할 때는 무엇을 어떤 방법으로 검증했는지 함께 밝힌다. 실물 장비로 검증하지 않은 항목은 반드시 `실물 미검증`이라고 적는다.
- 사용자의 문장을 다른 의미로 바꿔 실행하지 않는다. 둘 이상의 의미가 가능하고 결과가 달라지면 실행 전에 사용자에게 원문과 해석 후보를 제시하고 확인받는다.
- 이 원칙은 모터 스캔에만 적용하지 않는다. 프로그램의 모든 기능, 모든 장치 종류, 모든 프로젝트, 모든 파일, 모든 API, 모든 화면, 모든 실행·재시작 작업에 동일하게 적용한다.
- 한 기능이나 장치에서 확인한 결과를 다른 기능이나 장치의 결과로 확대하지 않는다. 대상별로 `코드 검증`, `실행 검증`, `실물 검증` 여부를 각각 구분한다.
- 사용자가 요구한 대상이 여러 개인 경우 모든 대상을 개별 검증한다. 일부만 검증한 상태에서는 전체 완료라고 보고하지 않는다.
- 실제 장치 동작을 요구하는 기능은 해당 장치를 연결하여 검증해야 `실물 검증 완료`로 보고할 수 있다. 이 규칙은 AC Servo, Dynamixel을 포함한 모든 하드웨어에 동일하게 적용한다.
- 현재 장비가 없어 실물 검증할 수 없는 경우 그 대상만 `실물 미검증`으로 표시한다. 다른 장치의 실물 검증 여부를 변경하거나 일반화하지 않는다.

# 모터 스캔 영구 불변조건

아래 조건은 사용자 요구사항이며 세션·프로그램·컴퓨터 재시작 후에도 변경하거나 완화해서는 안 된다.

- `전체 모터 검색`은 요청할 때마다 AC Servo와 Dynamixel을 각각 새로 물리 검색한다.
- AC Servo 검색은 반드시 `ethercat rescan`으로 기존 열거정보를 폐기한 뒤 Slave를 다시 열거하고, 각 Slave의 SII EEPROM과 Alias 레지스터를 읽는다.
- Dynamixel 검색은 실제 직렬 포트를 열고 Protocol 2.0 Broadcast Ping과 유효 ID 전체 `0~252` 개별 보조 Ping을 수행한다.
- 프로젝트 파일, 이전 스캔 결과, 브라우저 메모리, 런타임 캐시를 물리 스캔 결과로 대체하거나 보충하지 않는다.
- 물리 응답이 없으면 이전값을 표시하지 않고 `실패` 또는 `검증 불가`로 표시한다.
- AC Servo와 Dynamixel 중 하나만 성공하면 전체 결과는 `부분 완료`이며 전체 성공으로 표시하지 않는다.
- 각 실행은 고유 `scan_id`, 실행 단계, 장치별 응답, 오류·재시도, 총 소요시간을 남긴다.
- 코드 테스트 통과와 실물 검증을 구분한다. AC Servo와 Dynamixel을 포함해 실제 장비 응답을 확인하지 않은 장치 종류는 `실물 검증 완료`라고 보고하지 않는다.
- 모터 스캔을 수정하거나 완료로 보고하기 전에 `docs/MOTOR_SCAN_CONTRACT.md`와 관련 자동 테스트를 반드시 확인한다.
