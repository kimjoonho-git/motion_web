# 적용 범위 및 작업 원칙

- 이 규칙은 현재 프로젝트의 모든 코드, 파일, 기능, 장치, API, 화면, 실행 및 재시작 작업에 적용한다.
- 사용자 입력에 여러 해석이 가능하고 결과가 달라질 수 있으면 해석 후보를 제시하고 확인받은 후 작업한다.
- 사용자의 최신 질문에 먼저 답하고, 명시된 요청과 확인 가능한 사실만 근거로 판단한다.
- 검증 범위를 흐리는 표현을 사용하지 않고 `확인됨`, `미확인`, `부분 완료`, `실패`, `검증 불가`를 구분한다.
- 완료 보고에는 검증 대상, 방법 및 결과를 포함한다.
- 검증 상태는 `코드 검증`, `실행 검증`, `실물 검증`으로 구분하고, 실제 장비로 확인하지 않은 항목은 `실물 미검증`으로 표시한다.
- 여러 기능이나 장치가 대상이면 각각 검증하며 일부 결과를 전체 결과로 확대하지 않는다.

# 명칭 및 설명 문구

- 기능, 버튼, 상태, 장치, 파일, 노드, 토픽 및 작업 단계는 코드와 화면에서 사용하는 정확한 명칭으로 표기한다.
- 같은 기능에는 같은 명칭을 사용하고, 동작이 다른 기능을 같은 명칭으로 표현하지 않는다.
- 설명은 서술형 문장보다 `대상 · 작업 · 결과` 형식의 짧은 용어와 단계로 표시한다.
- 불필요한 수식어, 모호한 약칭, 기능 범위를 확대하는 표현 및 물리 상태로 오해할 수 있는 표현을 사용하지 않는다.
- 안전 조건, 실패 원인 또는 사용자 확인이 필요한 경우에만 필요한 문장형 설명을 추가한다.

# 프로젝트 중심 관리 및 데이터 격리

- 프로그램의 설정과 작업 데이터는 현재 활성화된 프로젝트 파일을 기준으로 관리한다.
- 프로젝트별 데이터, 실행 상태 및 캐시는 서로 격리한다.
- 다른 프로젝트의 데이터를 현재 프로젝트의 기본값이나 대체값으로 사용하지 않는다.
- 프로젝트를 전환할 때 이전 프로젝트의 데이터와 상태를 새 프로젝트에 전달하지 않는다.
- 프로젝트 간 데이터 복사, 병합 또는 공유는 사용자가 대상과 범위를 명시한 경우에만 수행한다.
- 공유 데이터는 전역 데이터로 명확하게 정의하고 프로젝트 데이터와 분리한다.
- 비동기 작업 결과는 작업을 시작한 프로젝트에만 적용한다.
- 관련 기능을 수정하면 서로 다른 두 개 이상의 프로젝트로 데이터 격리를 검증한다.

# motion_system 보호 및 모터 제어 통로

- `src/motion_system`의 코드, 설정, 인터페이스 및 실행 구조는 사용자가 수정 대상과 범위를 명시한 경우에만 수정한다.
- 일반적인 기능 구현이나 오류 수정 요청을 `src/motion_system` 수정 지시로 확대하지 않는다.
- 모터 통신 및 제어는 `motion_system`을 단일 통로로 사용한다.
- `motion_system`을 우회하는 통신 또는 제어 경로를 임의로 추가하거나 사용하지 않는다.
- 예외 경로는 사유, 대상 및 범위를 설명하고 사용자의 명시적인 지시를 받은 경우에만 사용한다.

# Git 작업 범위

- 사용자가 Git 작업 대상을 지정하지 않으면 `/home/joonho_test/ros2_ws`의 `motion_control_web`만 대상으로 한다.
- 일반적인 Git, 커밋, 브랜치, 푸시, `main` 및 복원 작업에 `src/motion_system`과 그 내부 저장소를 포함하지 않는다.
- `src/motion_system` 관련 Git 작업은 사용자가 `모션시스템`을 작업 대상으로 명시한 경우에만 수행한다.

# 모터 스캔 영구 불변조건

다음 조건은 세션, 프로그램 또는 컴퓨터 재시작 후에도 변경하거나 완화하지 않는다.

- `전체 모터 검색`을 실행할 때마다 AC Servo와 Dynamixel을 각각 새로 물리 검색한다.
- AC Servo는 `ethercat rescan`으로 기존 열거정보를 폐기한 후 Slave를 다시 열거하고 각 Slave의 SII EEPROM과 Alias 레지스터를 읽는다.
- Dynamixel은 실제 직렬 포트를 열고 Protocol 2.0 Broadcast Ping과 ID `0~252` 개별 보조 Ping을 수행한다.
- 프로젝트 파일, 이전 스캔 결과, 브라우저 메모리 및 런타임 캐시를 물리 스캔 결과로 대체하거나 보충하지 않는다.
- 물리 응답이 없으면 이전 값을 표시하지 않고 `실패` 또는 `검증 불가`로 표시한다.
- 한 장치 종류만 성공하면 전체 결과는 `부분 완료`로 표시한다.
- 각 실행은 고유 `scan_id`, 실행 단계, 장치별 응답, 오류, 재시도 및 총 소요시간을 기록한다.
- 실제 장비 응답을 확인하지 않은 장치 종류는 `실물 검증 완료`로 보고하지 않는다.
- 모터 스캔을 수정하거나 완료로 보고하기 전에 `docs/MOTOR_SCAN_CONTRACT.md`와 관련 자동 테스트를 확인한다.

# 코드 수정 후 안내

- 코드 수정 완료 보고 마지막에 사용자가 수행할 테스트 준비를 한두 줄로 안내한다.
- 빌드, 전체 프로그램 재시작 및 브라우저 `Ctrl+F5` 중 필요한 작업만 안내하고 이미 완료한 작업은 다시 요구하지 않는다.
