version: 2

# 프로젝트 파일과 분리된 PC 전역 DDS 그룹 설정 예시입니다.
pc_id: pc-a
display_name: PC A
enabled: false
group_id: ''
dds_domain_id: 21

heartbeat_sec: 0.5
warning_timeout_sec: 1.5
peer_timeout_sec: 3.0
start_lead_sec: 0.5
schedule_ack_margin_sec: 0.1
max_trigger_sync_uncertainty_ms: 5.0
trigger_sync_samples: 5
prepare_timeout_sec: 6.0
trigger_report_timeout_sec: 1.0

# 연동할 PC들에 같은 group_id와 dds_domain_id를 저장한 뒤,
# 각 PC 웹에서 사용자가 직접 `그룹 참가`를 누릅니다.
